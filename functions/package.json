{
  "name": "cuentas-compartidas-functions",
  "description": "Firebase Cloud Functions para notificaciones push",
  "engines": { "node": "18" },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "logs":   "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin":     "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
